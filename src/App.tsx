import { BrowserRouter } from "react-router";
import RouterView from "./router/RouterView";

function App() {
  return (
    <BrowserRouter>
      <RouterView />
    </BrowserRouter>
  );
}

export default App;
